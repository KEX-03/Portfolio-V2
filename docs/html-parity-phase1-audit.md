# Phase 1 Audit: `Portfolio v2.html` -> React + Vite

## Source Of Truth
- File: `H:\PORTFOLIO\Claude design WEBDEV PORTFOLIO\Portfolio web vivek\Portfolio v2.html`
- Size: 54,656 bytes
- Lines: 1,152

## Dependencies Found In Source HTML
- External fonts only (Google Fonts):
  - Instrument Serif (italics included)
  - Caveat (500, 700)
  - JetBrains Mono (400, 500, 700)
  - Plus Jakarta Sans (400, 500, 600, 700)
- No third-party JS libraries referenced in the HTML.
- All interactions are vanilla JS.

## Full Interaction Inventory (Must Reproduce 1:1)
- Custom cursor ring + cursor dot with smoothing loop (`requestAnimationFrame`)
- Cursor spotlight glow following mouse via CSS custom properties (`--spot-x`, `--spot-y`)
- Hover cursor expansion on links/buttons and click shrink behavior
- Magnetic hover translation on `[data-magnet]` elements
- Scroll progress top tape bar with dynamic width (`--p`)
- Live IST clock (`Intl.DateTimeFormat`, refresh every 30s)
- Hero role text rotator with fade/translate timing
- Code typing effect with HTML token-aware stepping
- Infinite tech marquee with pause on hover
- Infinite looping projects carousel:
  - tripled data strategy
  - prev/next controls
  - normalize index on transition end
  - pointer drag behavior
  - arrow key navigation
  - auto-advance timer and hover pause
  - card 3D tilt on mouse move
- Theme toggle (`paper` <-> `night`) via `data-theme` on `html`
- Scrollspy active nav link updates based on scroll offset
- Process-step connecting SVG arrows generated by JS + `IntersectionObserver` reveal
- Konami code easter egg:
  - toast reveal/hide
  - sparkle particle burst
- Console easter-egg logs
- Reduced-motion media query behavior

## Structure Inventory
- Sticky top nav with logo, links, availability status, theme button, CTA
- Hero with:
  - heading highlights
  - scribble SVG draw animation
  - typed code card
  - collage/photo card/polaroid styling
- Rotated marquee band
- Skills tech strip section
- Featured projects header + looping carousel
- About + Process dual panel section
- Connect section + socials + sticky note style block
- Footer + hidden toast layer

## CSS/Animation Inventory
- Root token system + night theme overrides
- CSS `@property` usage for spotlight coordinates
- Paper grain overlay + spotlight overlay layers
- Multiple keyframes: `pulse`, `wave`, `hl-in`, `draw`, `spinSlow`, `twinkle`, `blink`, `scroll-x`, `spk`
- Responsive breakpoints:
  - `max-width: 980px`
  - `max-width: 640px`
  - `prefers-reduced-motion: reduce`
  - `pointer: coarse`

## Asset/Content Notes
- Inline SVG icons used heavily throughout the HTML.
- Some glyph/emoji/text symbols appear mojibake in terminal output (encoding display issue). During migration we should preserve the intended UTF-8 symbols exactly from source content.

## React Conversion Plan
1. Keep Vite + React + TypeScript setup.
2. Build a dedicated parity page container and isolate source-specific styles in one parity stylesheet first.
3. Split into components only where safe (no visual/behavior change):
   - `Nav`, `Hero`, `MarqueeBand`, `TechStrip`, `ProjectsCarousel`, `AboutProcess`, `Connect`, `Footer`, `Toast`
4. Move JS behavior into React hooks with strict cleanup:
   - global pointer/scroll/keydown listeners
   - timers/intervals
   - `IntersectionObserver`
   - `requestAnimationFrame` loops
5. Preserve exact timing curves, spacing, and responsive breakpoints from source.
6. Validate parity section-by-section before continuing.

## Library Decision
- Additional libraries are **not required** for 1:1 parity because original behavior is vanilla JS + CSS.
- Keep current project dependencies; do not add motion libraries for this migration unless a parity blocker appears.

## Phase Execution Roadmap
- Phase 2:
  - global reset/token/theme/font setup parity
  - page scaffold and base layers (grain, spotlight, cursor shells, progress bar)
  - shared utility hooks and constants
- Phase 3:
  - nav + hero (including typed code + role rotator + magnetic + hero interactions)
- Phase 4:
  - marquee band + tech strip infinite marquee
- Phase 5:
  - projects carousel with all interactions
- Phase 6:
  - about/process/connect/footer + arrows + toast
- Phase 7:
  - theme toggle, scrollspy, konami, console easter egg
- Final phase:
  - pixel parity sweep, responsive diff pass, a11y checks, perf polish, cleanup
